import {h} from 'preact';

export default () => (
  <div>
    <h1>Could not find that page!</h1>
    <a href="/" class="mdc-button mdc-button--raised mdc-button--accent">Go to home page</a>
  </div>
);
